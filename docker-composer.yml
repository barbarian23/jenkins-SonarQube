version: '3'

networks:
  jenkins:

services:
  jenkins:
    image: jenkins
    container_name: jenkins
    networks:
      - jenkins
    volumes:
      - ./jenkins:/usr/share/elasticsearch/data
    environment:
      - xpack.security.enabled=false
      - discovery.type=single-node
    ports:
      - 9200:9200